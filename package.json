{
  "name": "relayforge-automation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-first PLC programming, deterministic simulation, and HMI design in vanilla JavaScript with WebGPU rendering.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node tools/build-standalone.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
